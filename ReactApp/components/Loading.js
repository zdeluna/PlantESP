import React from 'react';
import {Text} from 'react-native';
import {Center} from './Center';

const Loading = () => {
    return (
        <Center>
            <Text>Loading</Text>
        </Center>
    );
};

export default Loading;
